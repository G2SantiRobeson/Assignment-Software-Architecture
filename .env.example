# Local Docker Compose configuration. Copy this file to .env and replace the
# password before starting the stack. The .env file is intentionally ignored.
POSTGRES_DB=book_reviews_development
POSTGRES_USER=book_reviews
POSTGRES_PASSWORD=replace-with-a-local-password
APP_PORT=3000
